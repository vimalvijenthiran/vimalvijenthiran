
/*#include <stdio.h>
#include <math.h>
int main() {
 int a,b,x,l,s,z;
 int e=M_E;
 scanf("%d",&a);
 scanf("%d",&b);
 scanf("%d",&x);
 printf("\n");
 l=log((b+x)/2);
 s=sin(x+b);
 z=((e^(a*x) - e^(-a*x))/2)+s*(x+b)*a*l;
 printf("%d",z);
 return 0;
}*/
#include <stdio.h>
/* Test some Booleans. */
int main()
{
int whatever = 1;
if (whatever)
{
printf(“Is that true, Homer? n”);
}
else
{
printf(“No, Marge. n”);
}
return 0;
}
